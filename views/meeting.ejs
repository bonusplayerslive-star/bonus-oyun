<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
    <title>BPL | STRATEJİK OPERASYON MERKEZİ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&family=Rajdhani:wght@500;700&display=swap" rel="stylesheet">
    <style>
        :root { --neon-turq: #00f2ff; --neon-red: #ff004c; --bg-dark: #050a0f; }
        
        body { 
            background: var(--bg-dark); color: #fff; height: 100vh; margin: 0; 
            font-family: 'Rajdhani', sans-serif; overflow: hidden;
            display: flex; flex-direction: column;
            padding: env(safe-area-inset-top) 5px env(safe-area-inset-bottom) 5px;
        }

        /* Üst Panel */
        .top-nav { 
            height: 60px; border: 1px solid var(--neon-turq); border-radius: 10px;
            margin: 5px; background: rgba(0, 242, 255, 0.05);
            display: flex; align-items: center; padding: 0 15px; justify-content: space-between;
            box-shadow: 0 0 10px rgba(0, 242, 255, 0.2);
        }

        .master-layout { 
            flex: 1; 
            display: grid; 
            grid-template-columns: 280px 1fr 300px; 
            gap: 10px; 
            padding: 5px; 
            overflow: hidden;
        }

        @media (max-width: 992px) {
            body { overflow-y: auto; height: auto; }
            .master-layout { display: flex; flex-direction: column; height: auto; }
            .video-sidebar { order: 2; display: grid !important; grid-template-columns: 1fr 1fr; max-height: none; }
            .main-display { order: 1; height: 200px; }
            .chat-sidebar { order: 3; height: 400px; display: flex !important; }
        }

        .video-sidebar { 
            background: rgba(0,0,0,0.5); border: 1px solid rgba(0,242,255,0.3); 
            border-radius: 15px; padding: 10px; overflow-y: auto; display: flex; flex-direction: column; gap: 10px;
        }

        .v-slot { 
            width: 100%; aspect-ratio: 16/9; border-radius: 10px; border: 1px solid #333; 
            position: relative; background: #000; overflow: hidden; cursor: pointer;
            transition: transform 0.2s;
        }
        .v-slot:hover { transform: scale(1.02); border-color: var(--neon-turq); }
        .v-slot.active-host { border: 2px solid var(--neon-turq); box-shadow: 0 0 15px var(--neon-turq); }
        video { width: 100%; height: 100%; object-fit: cover; -webkit-transform: scaleX(-1); transform: scaleX(-1); pointer-events: none; }

        .main-display { 
            border: 1px solid rgba(0,242,255,0.3); border-radius: 15px;
            background: radial-gradient(circle, #0a1520 0%, #000 100%);
            display: flex; align-items: center; justify-content: center; position: relative;
        }

        .chat-sidebar { 
            border: 1px solid rgba(0,242,255,0.3); border-radius: 15px;
            background: rgba(0,0,0,0.8); display: flex; flex-direction: column;
        }
        .chat-messages { flex: 1; overflow-y: auto; padding: 10px; font-size: 0.9rem; }
        .msg-line { margin-bottom: 8px; border-bottom: 1px solid rgba(255,255,255,0.05); }
        .msg-nick { color: var(--neon-turq); font-weight: bold; }

        .neon-input { 
            background: transparent !important; border: 1px solid var(--neon-turq) !important;
            color: #fff !important; border-radius: 20px; font-size: 14px;
        }

        .btn-vip { 
            background: var(--neon-red); border: none; color: #fff; 
            box-shadow: 0 0 10px var(--neon-red); border-radius: 20px; font-family: 'Orbitron';
        }

        /* Sağ Tık / Profil Menüsü */
        #user-menu {
            position: fixed; display: none; background: #0a1520; border: 1px solid var(--neon-turq);
            border-radius: 8px; z-index: 1000; width: 180px; box-shadow: 0 0 20px #000;
        }
        .menu-item { padding: 12px; cursor: pointer; font-size: 13px; border-bottom: 1px solid #111; color: #fff; display: flex; align-items: center; }
        .menu-item i { width: 20px; }
        .menu-item:hover { background: var(--neon-turq); color: #000; }
        .menu-item.arena-btn:hover { background: var(--neon-red); color: #fff; }
    </style>
</head>
<body>

<nav class="top-nav">
    <div style="font-family:'Orbitron'; color:var(--neon-turq); font-size: 14px;">
        <i class="fas fa-microchip me-2"></i> <span id="room-name">YÜKLENİYOR...</span>
    </div>
    <div class="d-flex gap-2">
        <button id="mic-btn" onclick="toggleMic()" class="btn btn-outline-info btn-sm rounded-pill"><i class="fas fa-microphone"></i></button>
        <button id="vid-btn" onclick="toggleVid()" class="btn btn-outline-info btn-sm rounded-pill"><i class="fas fa-video"></i></button>
        <button onclick="exitRoom()" class="btn btn-danger btn-sm rounded-pill"><i class="fas fa-times"></i></button>
    </div>
</nav>

<div class="master-layout">
    <div class="video-sidebar" id="video-grid">
        <div class="v-slot active-host" id="local-slot" onclick="openMenu(event, myNick)">
            <video id="local-video" autoplay muted playsinline></video>
            <div style="position:absolute; bottom:5px; left:5px; background:rgba(0,0,0,0.6); font-size:10px; padding:2px 5px;">SEN (KOMUTAN)</div>
        </div>
    </div>

    <div class="main-display">
        <div class="text-center">
            <i class="fas fa-user-secret fa-4x mb-3" style="color:var(--neon-turq); opacity: 0.2;"></i>
            <div id="status-text" class="small text-muted">OPERASYON SAHASI AKTİF</div>
        </div>
    </div>

    <div class="chat-sidebar">
        <div class="p-2 text-center border-bottom border-info" style="font-family:'Orbitron'; font-size:12px; color:var(--neon-turq)">VIP TERMINAL</div>
        <div class="chat-messages" id="meeting-chat"></div>
        <div class="p-2 border-top border-info">
            <div class="input-group mb-2">
                <input type="text" id="m-input" class="form-control form-control-sm neon-input" placeholder="Mesaj...">
                <button onclick="sendMsg()" class="btn btn-sm btn-vip"><i class="fas fa-paper-plane"></i></button>
            </div>
            <div class="input-group">
                <input type="text" id="invite-nick" class="form-control form-control-sm neon-input" placeholder="Nick...">
                <button onclick="inviteUser()" class="btn btn-sm btn-info rounded-end">DAVET</button>
            </div>
        </div>
    </div>
</div>

<div id="user-menu">
    <div id="menu-title" class="p-2 text-center small border-bottom border-info text-info" style="font-weight:bold"></div>
    <div class="menu-item" onclick="handleAction('gift')"><i class="fas fa-gift me-2"></i>Hediye Gönder</div>
    <div class="menu-item arena-btn" style="color: var(--neon-red)" onclick="handleAction('arena')"><i class="fas fa-khanda me-2"></i>Arena Düellosu</div>
    <div class="menu-item text-danger" id="kick-opt" onclick="handleAction('kick')"><i class="fas fa-user-slash me-2"></i>Operasyondan At</div>
</div>

<script src="/socket.io/socket.io.js"></script>
<script src="https://unpkg.com/peerjs@1.5.2/dist/peerjs.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<script>
    const socket = io();
    const myNick = "<%= user.nickname %>";
    let myBpl = parseInt("<%= user.bpl %>");
    const params = new URLSearchParams(window.location.search);
    const roomID = params.get('room') || 'global';
    const role = params.get('role') || 'guest';
    const isHost = (role === 'host');

    document.getElementById('room-name').innerText = roomID.toUpperCase() + " SEKTÖRÜ";

    let localStream;
    let peer = new Peer();
    let selectedTarget = null;

    async function init() {
        try {
            localStream = await navigator.mediaDevices.getUserMedia({ video: true, audio: true });
            document.getElementById('local-video').srcObject = localStream;
        } catch (e) {
            Swal.fire("Donanım Hatası", "Kamera/Mikrofon izni reddedildi.", "error");
        }
    }

    peer.on('open', id => {
        socket.emit('join-meeting', { roomId: roomID, peerId: id, nickname: myNick });
    });

    socket.on('user-connected', data => {
        const call = peer.call(data.peerId, localStream, { metadata: { nickname: myNick } });
        call.on('stream', remoteStream => addVideo(data.peerId, remoteStream, data.nickname));
    });

    peer.on('call', call => {
        call.answer(localStream);
        call.on('stream', remoteStream => addVideo(call.peer, remoteStream, call.metadata?.nickname || "KOMUTAN"));
    });

    function addVideo(id, stream, nick) {
        if (document.getElementById(id)) return;
        const slot = document.createElement('div');
        slot.className = "v-slot"; slot.id = id;
        slot.onclick = (e) => openMenu(e, nick);
        const video = document.createElement('video');
        video.srcObject = stream; video.autoplay = true; video.setAttribute('playsinline', ''); 
        const tag = document.createElement('div');
        tag.className = "p-1 bg-dark text-info text-center";
        tag.style = "position:absolute; bottom:0; font-size:10px; width:100%; border-top:1px solid var(--neon-turq)";
        tag.innerText = nick;
        slot.append(video, tag);
        document.getElementById('video-grid').append(slot);
    }

    // --- PROFİL VE KRİTİK İŞLEM SİSTEMİ ---
    async function handleAction(type) {
        if (selectedTarget === myNick && type !== 'kick') {
            return Swal.fire("Hata", "Kendine işlem yapamazsın!", "info");
        }

        if (type === 'gift') {
            if (myBpl < 5500) {
                return Swal.fire("VIP ENGELİ", "Hediyeleşmek için en az 5500 BPL bakiyen olmalı!", "warning");
            }

            const { value: amount } = await Swal.fire({
                title: 'BPL Gönder',
                text: `${selectedTarget} komutana gönderilecek miktar:`,
                input: 'number',
                inputAttributes: { min: 1 },
                background: '#0a1520', color: '#fff',
                confirmButtonColor: '#00f2ff',
                showCancelButton: true
            });

            if (amount && amount > 0) {
                socket.emit('meeting-gift-send', { 
                    targetNick: selectedTarget, 
                    amount: parseInt(amount),
                    room: roomID
                });
            }
        } 
        else if (type === 'arena') {
            Swal.fire({
                title: 'Arena Düellosu',
                text: `${selectedTarget} komutanı Arena'ya davet etmek istiyor musun?`,
                icon: 'question',
                showCancelButton: true,
                confirmButtonText: 'DAVET ET',
                confirmButtonColor: '#ff004c',
                background: '#0a1520', color: '#fff'
            }).then(res => {
                if (res.isConfirmed) {
                    socket.emit('meeting-arena-invite-send', {
                        targetNick: selectedTarget,
                        bet: 25, // Sabit başlangıç bahsi, app.js'de ayarlanabilir
                        room: roomID
                    });
                    Swal.fire("Sinyal Gönderildi", "Rakip onayı bekleniyor...", "success");
                }
            });
        }
        else if (type === 'kick' && isHost) {
            socket.emit('host-action', { action: 'kick', targetNick: selectedTarget, room: roomID });
        }
    }

    // Menü Açma (Kamera Tıklaması)
    function openMenu(e, nick) {
        e.stopPropagation();
        selectedTarget = nick;
        const menu = document.getElementById('user-menu');
        document.getElementById('menu-title').innerText = nick.toUpperCase();
        
        // Host değilse ve başkasına bakıyorsa kick'i gizle
        document.getElementById('kick-opt').style.display = (isHost && nick !== myNick) ? 'block' : 'none';
        
        menu.style.display = 'block'; 
        menu.style.left = e.clientX + 'px'; 
        menu.style.top = e.clientY + 'px';
    }

    // Arena Davet Dinleyicisi
    socket.on('arena-invite-received', (data) => {
        Swal.fire({
            title: 'DÜELLO TALEBİ!',
            text: `${data.from} seni Arena'ya çağırıyor! Kabul ediyor musun?`,
            icon: 'warning',
            showCancelButton: true,
            confirmButtonText: 'KABUL ET',
            cancelButtonText: 'REDDET',
            background: '#0a1520', color: '#fff',
            confirmButtonColor: '#ff004c'
        }).then((res) => {
            if (res.isConfirmed) {
                if (localStream) localStream.getTracks().forEach(t => t.stop());
                socket.emit('arena-invite-accept', { from: data.from });
            }
        });
    });

    socket.on('force-arena-match', (data) => {
        if (localStream) localStream.getTracks().forEach(t => t.stop());
        window.location.href = `/arena?room=${data.room}`;
    });

    // BPL Güncelleme (Anlık)
    socket.on('update-bpl', (newBpl) => {
        myBpl = newBpl;
    });

    socket.on('error', (msg) => {
        Swal.fire("Hata", msg, "error");
    });

    // Chat ve Diğer
    function sendMsg() {
        const input = document.getElementById('m-input');
        if (input.value.trim()) {
            socket.emit('meeting-message', { room: roomID, text: input.value });
            input.value = "";
        }
    }

    socket.on('new-meeting-message', d => {
        const chat = document.getElementById('meeting-chat');
        chat.innerHTML += `<div class="msg-line"><span class="msg-nick">${d.sender}:</span> ${d.text}</div>`;
        chat.scrollTop = chat.scrollHeight;
    });

    function inviteUser() {
        const targetNick = document.getElementById('invite-nick').value.trim();
        if (!targetNick) return;
        socket.emit('meeting-invite-request', { to: targetNick, roomId: roomID });
        Swal.fire("Davet Gönderildi", `${targetNick} davet edildi.`, "success");
        document.getElementById('invite-nick').value = "";
    }

    function toggleMic() {
        const track = localStream.getAudioTracks()[0];
        track.enabled = !track.enabled;
        document.getElementById('mic-btn').classList.toggle('btn-danger', !track.enabled);
    }

    function toggleVid() {
        const track = localStream.getVideoTracks()[0];
        track.enabled = !track.enabled;
        document.getElementById('vid-btn').classList.toggle('btn-danger', !track.enabled);
    }

    function exitRoom() { 
        if (localStream) localStream.getTracks().forEach(t => t.stop());
        window.location.href = "/chat"; 
    }
    
    window.onclick = () => document.getElementById('user-menu').style.display = 'none';
    init();
</script>
</body>
</html>
