<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>BPL EKOSİSTEM | VR ve Bio-Teknoloji Geleceği</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Rajdhani:wght@300;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <style>
        :root {
            --neon-green: #39FF14;
            --neon-blue: #00d4ff;
            --dark-bg: #050505;
            --glass: rgba(0, 0, 0, 0.85);
            --gold: #d4af37;
        }

        body {
            background: var(--dark-bg) url('/caracter/page/welcome.jpg') no-repeat center center fixed;
            background-size: cover;
            color: #e0e0e0;
            font-family: 'Rajdhani', sans-serif;
            overflow-x: hidden;
            min-height: 100vh;
        }

        /* Navigasyon */
        .navbar {
            background: rgba(0,0,0,0.9);
            border-bottom: 1px solid rgba(57, 255, 20, 0.3);
            backdrop-filter: blur(10px);
            padding: 15px 0;
        }
        
        .navbar-brand img {
            height: 50px;
            filter: drop-shadow(0 0 5px var(--neon-green));
            transition: 0.3s;
        }

        .hero-section { padding: 140px 0 60px; text-align: center; }
        .hero-title {
            font-family: 'Orbitron', sans-serif;
            font-size: clamp(2.5rem, 8vw, 4.5rem);
            color: var(--neon-green);
            text-shadow: 0 0 20px rgba(57, 255, 20, 0.6);
            letter-spacing: 5px;
            font-weight: 700;
        }

        .glass-card {
            background: var(--glass);
            border: 1px solid rgba(57, 255, 20, 0.2);
            border-radius: 15px;
            padding: 30px;
            height: 100%;
            position: relative;
            box-shadow: 0 10px 30px rgba(0,0,0,0.8);
            backdrop-filter: blur(10px);
        }

        /* Token Grafik */
        .token-bar-container { 
            height: 220px; 
            display: flex; 
            align-items: flex-end; 
            justify-content: space-around; 
            margin-top: 40px;
            padding-bottom: 40px;
            border-bottom: 2px solid rgba(57, 255, 20, 0.1);
        }
        .token-bar { 
            width: 45px; 
            border-radius: 5px 5px 0 0; 
            position: relative; 
            transition: 1.5s;
            cursor: pointer;
        }
        .bar-label { position: absolute; top: -30px; width: 100%; text-align: center; font-weight: bold; font-family: 'Orbitron'; font-size: 0.8rem;}
        .bar-name { position: absolute; bottom: -35px; width: 100%; text-align: center; font-size: 0.8rem; color: var(--neon-green); white-space: nowrap;}

        /* Butonlar */
        .btn-neon-green {
            background: transparent; border: 2px solid var(--neon-green); color: var(--neon-green);
            font-family: 'Orbitron', sans-serif; font-weight: bold; padding: 12px 30px;
            letter-spacing: 2px; transition: 0.4s;
        }
        .btn-neon-green:hover { background: var(--neon-green); color: #000; box-shadow: 0 0 20px var(--neon-green); }

        /* Auth Panel */
        .auth-overlay {
            display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.95); z-index: 9999; justify-content: center; align-items: center;
            backdrop-filter: blur(15px);
        }
        .custom-input {
            background: rgba(255,255,255,0.05); border: 1px solid rgba(57,255,20,0.2);
            color: white !important; border-radius: 8px; padding: 12px; margin-bottom: 15px;
        }
        .custom-input:focus { background: rgba(255,255,255,0.1); border-color: var(--neon-green); box-shadow: 0 0 10px rgba(57, 255, 20, 0.3); }

        .contract-address {
            background: rgba(57, 255, 20, 0.05); padding: 15px; border-radius: 8px;
            font-family: 'Courier New', monospace; border: 1px dashed var(--neon-green); cursor: pointer;
            word-break: break-all; transition: 0.3s; color: var(--neon-green);
        }

        @media (max-width: 768px) { .token-bar { width: 30px; } }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <img src="/caracter/page/logo.jpeg" alt="BPL LOGO" onerror="this.src='/caracter/page/welcome.jpg'">
            </a>
            <div class="ms-auto">
                <% if (!locals.user) { %>
                    <button class="btn btn-outline-info btn-sm me-2 fw-bold" onclick="openAuth('login')">GİRİŞ YAP</button>
                    <button class="btn btn-neon-green btn-sm" onclick="openAuth('register')">ÜYE OL</button>
                <% } else { %>
                    <a href="/profil" class="btn btn-neon-green btn-sm">KOMUTA MERKEZİ</a>
                    <a href="/logout" class="btn btn-outline-danger btn-sm ms-2">SİSTEMDEN ÇIK</a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="container hero-section">
        <h1 class="hero-title">BPL EKOSİSTEMİ</h1>
        <p class="lead text-uppercase fw-bold" style="letter-spacing: 4px; color: #888;">Oyuncuların Efsaneye Dönüştüğü Yer</p>
        
        <div class="row g-4 mt-5">
            <div class="col-lg-6">
                <div class="glass-card text-start">
                    <h4 class="mb-4" style="color: var(--neon-green); font-family: 'Orbitron';">TOKEN DAĞILIMI</h4>
                    <div class="token-bar-container">
                        <div class="token-bar bg-success" style="height: 75%;"><span class="bar-label">75%</span><span class="bar-name">Şirket</span></div>
                        <div class="token-bar bg-info" style="height: 15%;"><span class="bar-label">15%</span><span class="bar-name">Piyasa</span></div>
                        <div class="token-bar bg-warning" style="height: 5%;"><span class="bar-label">5%</span><span class="bar-name">Oyunlar</span></div>
                        <div class="token-bar bg-danger" style="height: 5%;"><span class="bar-label">5%</span><span class="bar-name">Reklam</span></div>
                    </div>
                    <p class="mt-5 small text-muted text-center">* BPL Akıllı Kontrat v2.0 algoritmik dağıtımıdır.</p>
                </div>
            </div>
            
            <div class="col-lg-6">
                <div class="glass-card text-start">
                    <h4 class="text-warning mb-3" style="font-family: 'Orbitron';">KONTRAT ADRESİ (BEP20)</h4>
                    <p class="small text-secondary">Kazançlarınızı almak için MetaMask veya TrustWallet'a ekleyin:</p>
                    <div class="contract-address mb-4" onclick="copyContract()">
                        <i class="far fa-copy me-2"></i>0x9f63e92E8B316b7119b4586998966dF4446Dc754
                    </div>
                    
                    <ul class="list-unstyled mt-4" style="color: #bbb;">
                        <li class="mb-2"><i class="fas fa-shield-alt text-success me-2"></i> Kayıtta Anında 2500 BPL Bonus</li>
                        <li class="mb-2"><i class="fas fa-video text-success me-2"></i> Özel Sesli ve Görüntülü Odalar</li>
                        <li class="mb-2"><i class="fas fa-gift text-success me-2"></i> Küresel Hediye ve Transfer Sistemi</li>
                        <li class="mb-2"><i class="fas fa-wallet text-success me-2"></i> Güvenli BEP20 Çekim Altyapısı</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <div class="auth-overlay" id="authOverlay">
        <div class="glass-card" style="width: 100%; max-width: 400px;">
            <div class="text-center mb-4">
                <img src="/caracter/page/logo.jpeg" style="height: 60px;" onerror="this.src='/caracter/page/welcome.jpg'">
                <h3 id="authTitle" class="mt-3" style="color: var(--neon-green); font-family: 'Orbitron';">ERİŞİM PANELİ</h3>
            </div>
            
            <form id="authForm" method="POST">
                <div id="nicknameGroup">
                    <label class="small text-success fw-bold mb-1">OPERATÖR TAKMA ADI</label>
                    <input type="text" name="nickname" id="nicknameInput" class="form-control custom-input" placeholder="Kod adınızı belirleyin...">
                </div>
                
                <label class="small text-success fw-bold mb-1">E-POSTA ADRESİ</label>
                <input type="email" name="email" class="form-control custom-input" placeholder="e-posta@adresiniz.com" required>
                
                <label class="small text-success fw-bold mb-1">GÜVENLİ ŞİFRE</label>
                <input type="password" name="password" class="form-control custom-input" placeholder="••••••••" required>

                <div class="text-end mb-3" id="forgotPasswordLink">
                    <a href="#contact" onclick="closeAuth()" class="text-info small text-decoration-none">
                        <i class="fas fa-key me-1"></i>Şifremi Unuttum?
                    </a>
                </div>

                <button type="submit" class="btn btn-neon-green w-100 py-3 mt-2" id="authSubmitBtn">SİSTEMİ BAŞLAT</button>
                <button type="button" class="btn btn-link text-secondary w-100 mt-2 text-decoration-none small" onclick="closeAuth()">İŞLEMİ İPTAL ET</button>
            </form>
        </div>
    </div>

    <section id="contact" class="py-5">
        <div class="container border border-success p-4 rounded bg-black shadow-lg" style="max-width: 800px;">
            <h2 class="text-center text-success mb-4" style="font-family: 'Orbitron';">BPL DESTEK MERKEZİ</h2>
            <form id="helpForm">
                <div class="row">
                    <div class="col-md-6 mb-3">
                        <input type="email" id="helpEmail" class="form-control bg-dark text-white border-secondary" placeholder="Kayıtlı E-posta Adresiniz" required>
                    </div>
                    <div class="col-md-6 mb-3">
                        <select id="helpSubject" class="form-select bg-dark text-white border-secondary">
                            <option value="Genel Destek">Genel Destek</option>
                            <option value="Sifre Islemleri">Şifremi Unuttum / Yenileme</option>
                            <option value="Talep Iptali">Çekim Talebi İptali</option>
                        </select>
                    </div>
                </div>
                <div class="mb-3">
                    <textarea id="helpMessage" class="form-control bg-dark text-white border-secondary" rows="4" placeholder="Mesajınızın detaylarını buraya yazın..." required></textarea>
                </div>
                <button type="submit" class="btn btn-outline-success w-100">MESAJI MERKEZE GÖNDER</button>
            </form>
        </div>
    </section>

    <script>
        function openAuth(type) {
            const overlay = document.getElementById('authOverlay');
            const title = document.getElementById('authTitle');
            const nickGroup = document.getElementById('nicknameGroup');
            const nickInput = document.getElementById('nicknameInput');
            const form = document.getElementById('authForm');
            const forgotLink = document.getElementById('forgotPasswordLink');
            const submitBtn = document.getElementById('authSubmitBtn');
            
            overlay.style.display = 'flex';
            
            if(type === 'login') {
                form.action = '/login'; 
                title.innerText = 'SİSTEME GİRİŞ';
                submitBtn.innerText = 'OTURUMU AÇ';
                nickGroup.style.display = 'none'; 
                nickInput.required = false;
                forgotLink.style.display = 'block';
            } else {
                form.action = '/register'; 
                title.innerText = 'YENİ KAYIT';
                submitBtn.innerText = 'KAYDI TAMAMLA';
                nickGroup.style.display = 'block'; 
                nickInput.required = true;
                forgotLink.style.display = 'none';
            }
        }
        
        function closeAuth() { document.getElementById('authOverlay').style.display = 'none'; }
        
        function copyContract() {
            const address = '0x9f63e92E8B316b7119b4586998966dF4446Dc754';
            navigator.clipboard.writeText(address).then(() => {
                Swal.fire({ icon: 'success', title: 'Kopyalandı!', text: 'Kontrat adresi panoya eklendi.', background: '#000', color: '#fff' });
            });
        }

        // Destek Formu Gönderimi
        document.getElementById('helpForm').addEventListener('submit', function(e) {
            e.preventDefault();
            const data = {
                email: document.getElementById('helpEmail').value,
                subject: document.getElementById('helpSubject').value,
                message: document.getElementById('helpMessage').value
            };

            fetch('/api/help-request', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(data)
            })
            .then(res => res.json())
            .then(res => {
                if(res.success) {
                    Swal.fire({ icon: 'success', title: 'İletildi', text: 'Talebiniz alındı. E-postanızı kontrol edin.', background: '#000', color: '#fff' });
                    document.getElementById('helpForm').reset();
                } else {
                    Swal.fire({ icon: 'error', title: 'Hata', text: res.error, background: '#000', color: '#fff' });
                }
            });
        });

        window.onclick = function(event) { if (event.target == document.getElementById('authOverlay')) closeAuth(); }
    </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

