<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BonusPlayersLive | Geleceğin Ekosistemi</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { 
            background: #050505 url('/caracter/page/welcome.jpg') no-repeat center center fixed; 
            background-size: cover; 
            color: white; 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .glass-card { 
            background: rgba(0, 0, 0, 0.75); 
            border: 1px solid rgba(57, 255, 20, 0.2); 
            backdrop-filter: blur(15px); 
            border-radius: 20px; 
            padding: 30px; 
            box-shadow: 0 8px 32px rgba(0,0,0,0.5);
        }
        .neon-text { color: #39FF14; text-shadow: 0 0 10px rgba(57, 255, 20, 0.5); }
        
        /* Şık Giriş Alanları */
        .custom-input {
            background: rgba(255, 255, 255, 0.05) !important;
            border: 1px solid rgba(57, 255, 20, 0.3) !important;
            color: white !important;
            border-radius: 10px !important;
            padding: 12px !important;
            margin-bottom: 15px;
        }
        .custom-input:focus {
            border-color: #39FF14 !important;
            box-shadow: 0 0 15px rgba(57, 255, 20, 0.4) !important;
        }
        ::placeholder { color: rgba(255,255,255,0.4) !important; font-style: italic; }

        .auth-overlay {
            display: none;
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.9);
            z-index: 9999;
            justify-content: center;
            align-items: center;
        }
    </style>
</head>
<body>

<div class="container py-5">
    <div class="text-center mb-5">
        <h1 class="display-3 neon-text fw-bold">BPL ECOSYSTEM</h1>
        <p class="lead">Geleceğin VR ve Sağlık Teknolojilerine Hoş Geldiniz</p>
        <div class="d-flex justify-content-center gap-3 mt-4">
            <button class="btn btn-outline-success px-4 py-2" onclick="openAuth('login')">GİRİŞ YAP</button>
            <button class="btn btn-success px-4 py-2" onclick="openAuth('register')">HAYDİ KATIL (2500 BPL)</button>
        </div>
    </div>

    <div class="row g-4 mt-5">
        <div class="col-md-4">
            <div class="glass-card h-100">
                <h4 class="neon-text">VR VİZYONU 2026</h4>
                <p>Kendi VR gözlüklerinizle Arena'da bizzat dövüşeceğiniz Meta-Dünya projemiz hızla ilerliyor.</p>
            </div>
        </div>
        <div class="col-md-4">
            <div class="glass-card h-100">
                <h4 class="neon-text">EKONOMİK DEVRİM</h4>
                <p>%75 Şirket, %15 Borsa, %5 Oyun, %5 Reklam. Tüm transferler BEP20 ağında şeffaf.</p>
            </div>
        </div>
        <div class="col-md-4">
            <div class="glass-card h-100">
                <h4 class="neon-text">SAĞLIK VE BİLİM 2028</h4>
                <p>Kanser araştırmaları ve hammadde tedariği ile insanlık için kalıcı değerler üretiyoruz.</p>
            </div>
        </div>
    </div>

    <div class="mt-5 row justify-content-center">
        <div class="col-md-8">
            <div class="glass-card border-info">
                <h4 class="text-info mb-4 text-center">İLETİŞİM MERKEZİ</h4>
                <form action="/contact" method="POST">
                    <div class="row g-3">
                        <div class="col-md-5">
                            <input type="email" name="email" class="form-control custom-input" placeholder="E-posta Adresin..." required>
                        </div>
                        <div class="col-md-7">
                            <input type="text" name="note" maxlength="180" class="form-control custom-input" placeholder="Mesajın (Maks. 180 Karakter)..." required>
                        </div>
                        <div class="col-12 text-center">
                            <button type="submit" class="btn btn-info w-50">MESAJI GÖNDER</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<div class="auth-overlay" id="authOverlay">
    <div class="glass-card" style="width: 400px;">
        <h3 id="authTitle" class="neon-text text-center mb-4">ERİŞİM PANELİ</h3>
        <form id="authForm" method="POST">
            <div id="nicknameGroup">
                <input type="text" name="nickname" class="form-control custom-input" placeholder="Kullanıcı Adı Belirle...">
            </div>
            <input type="email" name="email" class="form-control custom-input" placeholder="E-posta Adresini Yaz..." required>
            <input type="password" name="password" class="form-control custom-input" placeholder="Şifreni Gir..." required>
            
            <button type="submit" class="btn btn-success w-100 py-2 fw-bold">İŞLEMİ TAMAMLA</button>
            <button type="button" class="btn btn-link text-white w-100 mt-2" onclick="closeAuth()">Vazgeç</button>
        </form>
        
        <div id="forgotSection" class="text-center mt-3">
            <hr class="border-secondary">
            <p class="small text-secondary">Şifreni mi unuttun?</p>
            <form action="/forgot-password" method="POST">
                <input type="email" name="email" class="form-control custom-input" placeholder="Kayıtlı Mailini Yaz..." required>
                <button type="submit" class="btn btn-outline-warning btn-sm">ŞİFREMİ MAİL AT</button>
            </form>
        </div>
    </div>
</div>

<script>
    function openAuth(type) {
        document.getElementById('authOverlay').style.display = 'flex';
        const form = document.getElementById('authForm');
        const title = document.getElementById('authTitle');
        const nickGroup = document.getElementById('nicknameGroup');
        const forgot = document.getElementById('forgotSection');

        if(type === 'login') {
            form.action = '/login';
            title.innerText = 'SİSTEME GİRİŞ';
            nickGroup.style.display = 'none';
            forgot.style.display = 'block';
        } else {
            form.action = '/register';
            title.innerText = 'YENİ KAYIT';
            nickGroup.style.display = 'block';
            forgot.style.display = 'none';
        }
    }

    function closeAuth() {
        document.getElementById('authOverlay').style.display = 'none';
    }
</script>

</body>
</html>
