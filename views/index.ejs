<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BPL EKOSİSTEM | Ultimate</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root { --neon-blue: #00f2ff; --neon-purple: #bc13fe; --dark-bg: #0a0b10; }
        body { font-family: 'Roboto', sans-serif; background: var(--dark-bg); color: white; margin: 0; }
        .hero-section { height: 100vh; display: flex; flex-direction: column; align-items: center; justify-content: center; }
        .main-title { font-family: 'Orbitron', sans-serif; font-size: 3rem; color: var(--neon-blue); text-shadow: 0 0 15px var(--neon-blue); margin-bottom: 30px; }
        .auth-container { background: rgba(255, 255, 255, 0.05); padding: 30px; border-radius: 15px; border: 1px solid var(--neon-purple); width: 320px; text-align: center; }
        .error-msg { color: #ff4d4d; font-size: 0.85rem; margin-bottom: 15px; display: none; }
        .input-group { margin-bottom: 15px; text-align: left; }
        .input-group label { display: block; margin-bottom: 5px; color: var(--neon-blue); }
        .input-group input { width: 100%; padding: 10px; border-radius: 5px; border: none; background: #1a1b26; color: white; border-bottom: 2px solid var(--neon-purple); box-sizing: border-box; }
        .btn-glow { width: 100%; padding: 12px; border: none; border-radius: 5px; background: var(--neon-purple); color: white; font-weight: bold; cursor: pointer; transition: 0.3s; }
        .btn-glow:hover { box-shadow: 0 0 15px var(--neon-purple); opacity: 0.9; }
        .switch-link { margin-top: 15px; font-size: 0.8rem; color: #888; cursor: pointer; display: block; }
    </style>
</head>
<body>

    <div class="hero-section">
        <h1 class="main-title">BPL ECOSYSTEM</h1>
        
        <div class="auth-container" id="loginBox">
            <h2 style="margin-bottom: 20px;">Giriş Yap</h2>
            <div id="loginError" class="error-msg">Hatalı Nickname veya Şifre!</div>
            <form action="/login" method="POST">
                <div class="input-group">
                    <label>Nickname</label>
                    <input type="text" name="nickname" placeholder="Kullanıcı adın..." required>
                </div>
                <div class="input-group">
                    <label>Şifre</label>
                    <input type="password" name="password" placeholder="******" required>
                </div>
                <button type="submit" class="btn-glow">Sisteme Bağlan</button>
            </form>
            <span class="switch-link" onclick="toggleAuth()">Hesabın yok mu? Kayıt Ol</span>
        </div>

        <div class="auth-container" id="registerBox" style="display: none;">
            <h2 style="margin-bottom: 20px;">Kayıt Ol</h2>
            <form action="/register" method="POST">
                <div class="input-group">
                    <label>Nickname</label>
                    <input type="text" name="nickname" placeholder="Harika bir isim seç..." required>
                </div>
                <div class="input-group">
                    <label>Şifre</label>
                    <input type="password" name="password" placeholder="En az 6 karakter" required>
                </div>
                <button type="submit" class="btn-glow">Ekosisteme Katıl</button>
            </form>
            <span class="switch-link" onclick="toggleAuth()">Zaten üye misin? Giriş Yap</span>
        </div>
    </div>

    <script>
        function toggleAuth() {
            const loginBox = document.getElementById('loginBox');
            const registerBox = document.getElementById('registerBox');
            if (loginBox.style.display === 'none') {
                loginBox.style.display = 'block';
                registerBox.style.display = 'none';
            } else {
                loginBox.style.display = 'none';
                registerBox.style.display = 'block';
            }
        }

        // URL'de hata varsa mesajı göster
        if (window.location.search.includes('error')) {
            document.getElementById('loginError').style.display = 'block';
        }
    </script>
</body>
</html>
