<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BPL ECOSYSTEM | The Future of VR & Bio-Tech</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Rajdhani:wght@300;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
        :root {
            --neon-green: #39FF14;
            --neon-blue: #00d4ff;
            --dark-bg: #050505;
            --glass: rgba(0, 0, 0, 0.85);
        }

        body {
            background: var(--dark-bg) url('/caracter/page/welcome.jpg') no-repeat center center fixed;
            background-size: cover;
            color: #e0e0e0;
            font-family: 'Rajdhani', sans-serif;
            overflow-x: hidden;
        }

        .navbar {
            background: rgba(0,0,0,0.9);
            border-bottom: 1px solid rgba(57, 255, 20, 0.3);
            backdrop-filter: blur(10px);
        }
        
        .navbar-brand img {
            height: 50px;
            filter: drop-shadow(0 0 5px var(--neon-green));
        }

        .hero-section {
            padding: 120px 0 60px;
            text-align: center;
        }
        .hero-title {
            font-family: 'Orbitron', sans-serif;
            font-size: 4rem;
            color: var(--neon-green);
            text-shadow: 0 0 20px rgba(57, 255, 20, 0.6);
            letter-spacing: 5px;
        }

        .glass-card {
            background: var(--glass);
            border: 1px solid rgba(57, 255, 20, 0.2);
            border-radius: 15px;
            padding: 25px;
            height: 100%;
            position: relative;
            box-shadow: 0 0 20px rgba(0,0,0,0.5);
        }

        .token-bar-container { 
            height: 200px; 
            display: flex; 
            align-items: flex-end; 
            justify-content: space-around; 
            margin-top: 30px;
            padding-bottom: 40px;
            border-bottom: 2px solid rgba(57, 255, 20, 0.1);
        }
        .token-bar { 
            width: 40px; 
            border-radius: 5px 5px 0 0; 
            position: relative; 
            transition: 1s ease-in-out;
        }
        .bar-label { 
            position: absolute; 
            top: -25px; 
            width: 100%; 
            text-align: center; 
            font-weight: bold; 
            font-size: 0.8rem;
        }
        .bar-name { 
            position: absolute; 
            bottom: -30px; 
            width: 100%; 
            text-align: center; 
            font-size: 0.75rem; 
            color: var(--neon-green);
            white-space: nowrap;
        }

        .btn-neon-green {
            background: transparent; border: 2px solid var(--neon-green); color: var(--neon-green);
            font-family: 'Orbitron', sans-serif; font-weight: bold; padding: 10px 25px;
        }
        .btn-neon-green:hover { background: var(--neon-green); color: #000; box-shadow: 0 0 15px var(--neon-green); }

        .auth-overlay {
            display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.95); z-index: 9999; justify-content: center; align-items: center;
        }
        .custom-input {
            background: rgba(255,255,255,0.05); border: 1px solid rgba(57,255,20,0.3);
            color: white; border-radius: 8px; padding: 12px; margin-bottom: 15px;
        }
        .custom-input:focus {
            background: rgba(255,255,255,0.1); border-color: var(--neon-green); color: white; box-shadow: none;
        }
        .contract-address {
            background: rgba(57, 255, 20, 0.1); padding: 10px; border-radius: 5px;
            font-family: monospace; border: 1px dashed var(--neon-green); cursor: pointer;
            word-break: break-all; transition: 0.3s;
        }
        .contract-address:hover { background: rgba(57, 255, 20, 0.2); }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <img src="/caracter/page/logo.jpeg" alt="BPL LOGO" onerror="this.src='/caracter/page/welcome.jpg'">
            </a>
            <div class="ms-auto">
                <% if (!user) { %>
                    <button class="btn btn-outline-info btn-sm me-2" onclick="openAuth('login')"><i class="fas fa-sign-in-alt"></i> SIGN IN</button>
                    <button class="btn btn-neon-green btn-sm" onclick="openAuth('register')">JOIN NOW</button>
                <% } else { %>
                    <a href="/profil" class="btn btn-neon-green btn-sm">DASHBOARD</a>
                    <a href="/logout" class="btn btn-outline-danger btn-sm">EXIT</a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="container hero-section">
        <h1 class="hero-title">BPL ECOSYSTEM</h1>
        <p class="lead text-uppercase" style="letter-spacing: 3px;">Where Gamers Evolve Into Legends</p>
        
        <div class="row g-4 mt-5">
            <div class="col-md-6 text-start">
                <div class="glass-card">
                    <h4 class="neon-text mb-4" style="color: var(--neon-green);">TOKEN ALLOCATION</h4>
                    <div class="token-bar-container">
                        <div class="token-bar bg-success" style="height: 75%;"><span class="bar-label">75%</span><span class="bar-name">Company</span></div>
                        <div class="token-bar bg-info" style="height: 15%;"><span class="bar-label">15%</span><span class="bar-name">Market</span></div>
                        <div class="token-bar bg-warning" style="height: 5%;"><span class="bar-label">5%</span><span class="bar-name">Gaming</span></div>
                        <div class="token-bar bg-danger" style="height: 5%;"><span class="bar-label">5%</span><span class="bar-name">Ads</span></div>
                    </div>
                </div>
            </div>
            
            <div class="col-md-6 text-start">
                <div class="glass-card">
                    <h4 class="text-warning mb-3">CONTRACT ADDRESS (BEP20)</h4>
                    <p class="small text-secondary">Add this to your MetaMask or TrustWallet to receive your earnings:</p>
                    <div class="contract-address" onclick="copyContract()">0xB47FD3EFAcDdB316685D14B89598D78B4ce25181</div>
                    <ul class="mt-4 small text-secondary list-unstyled">
                        <li class="mb-2"><i class="fas fa-check-circle text-success me-2"></i> Instant 2500 BPL Bonus on Registration</li>
                        <li class="mb-2"><i class="fas fa-check-circle text-success me-2"></i> Video/Voice Meeting Rooms</li>
                        <li class="mb-2"><i class="fas fa-check-circle text-success me-2"></i> Global Gifting System</li>
                        <li class="mb-2"><i class="fas fa-check-circle text-success me-2"></i> BEP20 Withdrawal Support</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <div class="auth-overlay" id="authOverlay">
        <div class="glass-card" style="width: 450px; border-color: var(--neon-green); height: auto;">
            <div class="text-center mb-4">
                <img src="/caracter/page/logo.jpeg" style="height: 60px;" onerror="this.src='/caracter/page/welcome.jpg'">
                <h3 id="authTitle" class="mt-3" style="color: var(--neon-green); font-family: 'Orbitron';">ACCESS PANEL</h3>
            </div>
            
            <form id="authForm" method="POST">
                <div id="nicknameGroup">
                    <label class="small text-success">OPERATOR NICKNAME</label>
                    <input type="text" name="nickname" id="nicknameInput" class="form-control custom-input" placeholder="Choose a name...">
                </div>
                
                <label class="small text-success">EMAIL ADDRESS</label>
                <input type="email" name="email" class="form-control custom-input" placeholder="name@example.com" required>
                
                <div id="passwordGroup">
                    <label class="small text-success">SECURE PASSWORD</label>
                    <input type="password" name="password" class="form-control custom-input" placeholder="••••••••" required>
                </div>

                <button type="submit" class="btn btn-neon-green w-100 py-3 mt-2">INITIALIZE SYSTEM</button>
                <button type="button" class="btn btn-link text-secondary w-100 mt-2" onclick="closeAuth()">ABORT MISSION</button>
            </form>
        </div>
    </div>

    <script>
        function openAuth(type) {
            document.getElementById('authOverlay').style.display = 'flex';
            const title = document.getElementById('authTitle');
            const nickGroup = document.getElementById('nicknameGroup');
            const nickInput = document.getElementById('nicknameInput');
            const form = document.getElementById('authForm');
            
            if(type === 'login') {
                form.action = '/login'; 
                title.innerText = 'CORE LOGIN';
                nickGroup.style.display = 'none'; 
                nickInput.required = false;
            } else {
                form.action = '/register'; 
                title.innerText = 'NEW RECRUIT';
                nickGroup.style.display = 'block'; 
                nickInput.required = true;
            }
        }
        
        function closeAuth() { document.getElementById('authOverlay').style.display = 'none'; }
        
        function copyContract() {
            navigator.clipboard.writeText('0xB47FD3EFAcDdB316685D14B89598D78B4ce25181');
            alert('Contract Address Copied to Clipboard!');
        }
    </script>
</body>
</html>
