<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BPL ECOSYSTEM | The Future of VR & Bio-Tech</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Rajdhani:wght@300;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
        :root {
            --neon-green: #39FF14;
            --neon-blue: #00d4ff;
            --dark-bg: #050505;
            --glass: rgba(0, 0, 0, 0.8);
        }

        body {
            background: var(--dark-bg) url('/caracter/page/welcome.jpg') no-repeat center center fixed;
            background-size: cover;
            color: #e0e0e0;
            font-family: 'Rajdhani', sans-serif;
            overflow-x: hidden;
        }

        /* Navigasyon ve Logo */
        .navbar {
            background: rgba(0,0,0,0.9);
            border-bottom: 1px solid rgba(57, 255, 20, 0.3);
            backdrop-filter: blur(10px);
        }
        .navbar-brand img {
            height: 50px;
            filter: drop-shadow(0 0 5px var(--neon-green));
        }

        /* Hero Section */
        .hero-section {
            padding: 120px 0 60px;
            text-align: center;
        }
        .hero-title {
            font-family: 'Orbitron', sans-serif;
            font-size: 4rem;
            font-weight: 700;
            color: var(--neon-green);
            text-shadow: 0 0 20px rgba(57, 255, 20, 0.6);
            letter-spacing: 5px;
        }

        /* Kart Yapıları */
        .glass-card {
            background: var(--glass);
            border: 1px solid rgba(57, 255, 20, 0.2);
            border-radius: 15px;
            padding: 25px;
            transition: all 0.4s ease;
            height: 100%;
            position: relative;
            overflow: hidden;
        }
        .glass-card:hover {
            border-color: var(--neon-green);
            transform: translateY(-10px);
            box-shadow: 0 0 30px rgba(57, 255, 20, 0.2);
        }
        .neon-border-blue { border-color: rgba(0, 212, 255, 0.3); }

        /* Butonlar */
        .btn-neon-green {
            background: transparent;
            border: 2px solid var(--neon-green);
            color: var(--neon-green);
            font-family: 'Orbitron', sans-serif;
            font-weight: bold;
            letter-spacing: 2px;
            transition: 0.3s;
            padding: 12px 30px;
        }
        .btn-neon-green:hover {
            background: var(--neon-green);
            color: #000;
            box-shadow: 0 0 20px var(--neon-green);
        }

        /* Tokenomics Grafiği (Dikey Kutular) */
        .token-bar-container { height: 250px; display: flex; align-items: flex-end; justify-content: space-around; margin-top: 20px; }
        .token-bar { width: 40px; border-radius: 5px 5px 0 0; position: relative; transition: 1s; }
        .bar-label { position: absolute; top: -30px; width: 100%; text-align: center; font-weight: bold; }
        .bar-name { position: absolute; bottom: -40px; width: 100%; text-align: center; font-size: 0.8rem; transform: rotate(-45deg); }

        /* Auth Overlay */
        .auth-overlay {
            display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.95); z-index: 9999; justify-content: center; align-items: center;
        }
        .custom-input {
            background: rgba(255,255,255,0.05); border: 1px solid rgba(57,255,20,0.3);
            color: white; border-radius: 8px; padding: 12px; margin-bottom: 15px;
        }
        .custom-input:focus { background: rgba(255,255,255,0.1); border-color: var(--neon-green); box-shadow: 0 0 10px var(--neon-green); color: white; }

        .contract-address {
            background: rgba(57, 255, 20, 0.1);
            padding: 10px;
            border-radius: 5px;
            font-family: monospace;
            word-break: break-all;
            border: 1px dashed var(--neon-green);
            cursor: pointer;
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <img src="/caracter/logo/logo.jpg" alt="BPL LOGO">
            </a>
            <div class="ms-auto">
                <button class="btn btn-outline-info btn-sm me-2" onclick="openAuth('login')"><i class="fas fa-sign-in-alt"></i> SIGN IN</button>
                <button class="btn btn-neon-green btn-sm" onclick="openAuth('register')">JOIN NOW</button>
            </div>
        </div>
    </nav>

    <div class="container hero-section">
        <h1 class="hero-title">BPL ECOSYSTEM</h1>
        <p class="lead text-uppercase tracking-widest" style="letter-spacing: 3px;">Where Gamers Evolve Into Legends</p>
        
        <div class="row g-4 mb-5">
            <div class="col-md-12">
                <div class="glass-card">
                    <h3 class="neon-text mb-3"><i class="fas fa-info-circle"></i> ABOUT BPL TOKEN</h3>
                    <p style="font-size: 1.1rem;">BPL is not just a token; it's the lifeblood of a massive vision. We start with gaming circulation and scale into high-tech healthcare by 2028. Every player in our arena is a partner in this revolution.</p>
                </div>
            </div>
        </div>

        <div class="row g-4">
            <div class="col-md-4">
                <div class="glass-card">
                    <div class="badge bg-success mb-2">PHASE 01 (2024-2025)</div>
                    <h4 class="text-info">CIRCULATION & V2</h4>
                    <p class="small text-start">Start with 6 legendary animals. Train, develop, and fight. With V2, we introduce new characters and advanced battle mechanics to boost token demand.</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="glass-card" style="border-color: #ff00ff;">
                    <div class="badge bg-danger mb-2">PHASE 02 (2026)</div>
                    <h4 style="color: #ff00ff;">VR REVOLUTION (V3)</h4>
                    <p class="small text-start">Real-time VR battles using BPL Token. Enter the Metaverse Arena with your own headset and experience the most addictive gameplay in the BEP20 ecosystem.</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="glass-card neon-border-blue">
                    <div class="badge bg-primary mb-2">PHASE 03 (2028)</div>
                    <h4 class="text-primary">BIO-HEALTH & R&D</h4>
                    <p class="small text-start">BPL scales into the medical sector. Providing essential raw materials for Cancer Research (Breast & Lung) and partnering with global universities.</p>
                </div>
            </div>
        </div>

        <div class="row mt-5">
            <div class="col-md-6">
                <div class="glass-card text-start">
                    <h4 class="neon-text">TOKEN ALLOCATION</h4>
                    <div class="token-bar-container">
                        <div class="token-bar bg-success" style="height: 75%;" title="Company"><span class="bar-label">75%</span><span class="bar-name">Company</span></div>
                        <div class="token-bar bg-info" style="height: 15%;" title="Exchange"><span class="bar-label">15%</span><span class="bar-name">Market</span></div>
                        <div class="token-bar bg-warning" style="height: 5%;" title="Gaming"><span class="bar-label">5%</span><span class="bar-name">Gaming</span></div>
                        <div class="token-bar bg-danger" style="height: 5%;" title="Marketing"><span class="bar-label">5%</span><span class="bar-name">Ads</span></div>
                    </div>
                </div>
            </div>
            <div class="col-md-6 text-start">
                <div class="glass-card">
                    <h4 class="text-warning">CONTRACT ADDRESS (BEP20)</h4>
                    <p class="small">Add this to your MetaMask or TrustWallet to receive your earnings:</p>
                    <div class="contract-address" onclick="copyContract()">
                        0xB47FD3EFAcDdB316685D14B89598D78B4ce25181
                    </div>
                    <ul class="mt-3 small text-secondary">
                        <li>* Instant 2500 BPL Bonus on Registration</li>
                        <li>* Video/Voice Meeting Rooms</li>
                        <li>* Global Gifting System</li>
                        <li>* BEP20 Withdrawal Support</li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="row mt-5 justify-content-center">
            <div class="col-md-10">
                <div class="glass-card border-info">
                    <h3 class="text-info text-center mb-4"><i class="fas fa-headset"></i> COMMAND CENTER</h3>
                    <form action="/contact" method="POST">
                        <div class="row">
                            <div class="col-md-4">
                                <input type="email" name="email" class="form-control custom-input" placeholder="Your Email..." required>
                            </div>
                            <div class="col-md-6">
                                <input type="text" name="note" maxlength="180" class="form-control custom-input" placeholder="Transmission Note (Max 180)..." required>
                            </div>
                            <div class="col-md-2">
                                <button type="submit" class="btn btn-info w-100 py-2">SEND</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <div class="auth-overlay" id="authOverlay">
        <div class="glass-card" style="width: 450px; border-color: var(--neon-green);">
            <div class="text-center mb-4">
                <img src="/caracter/logo/logo.png" style="height: 60px;">
                <h3 id="authTitle" class="neon-text mt-3">ACCESS PANEL</h3>
            </div>
            
            <form id="authForm" method="POST">
                <div id="nicknameGroup">
                    <label class="small text-success">OPERATOR NICKNAME</label>
                    <input type="text" name="nickname" class="form-control custom-input" placeholder="Choose a name...">
                </div>
                <label class="small text-success">EMAIL ADDRESS</label>
                <input type="email" name="email" id="mainEmail" class="form-control custom-input" placeholder="name@example.com" required>
                
                <div id="passwordGroup">
                    <label class="small text-success">SECURE PASSWORD</label>
                    <input type="password" name="password" class="form-control custom-input" placeholder="••••••••" required>
                </div>

                <button type="submit" class="btn btn-neon-green w-100 py-3 mt-2">INITIALIZE SYSTEM</button>
                <button type="button" class="btn btn-link text-secondary w-100 mt-2" onclick="closeAuth()">ABORT MISSION</button>
            </form>

            <div id="forgotSection" class="mt-4 pt-3 border-top border-secondary">
                <button class="btn btn-sm btn-outline-warning w-100" onclick="toggleForgot()">LOST PASSWORD?</button>
                <div id="forgotForm" style="display:none;" class="mt-3">
                    <form action="/forgot-password" method="POST">
                        <input type="email" name="email" class="form-control custom-input" placeholder="Enter Registered Email..." required>
                        <button type="submit" class="btn btn-warning btn-sm w-100">SEND PASSWORD TO MAIL</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script>
        function openAuth(type) {
            document.getElementById('authOverlay').style.display = 'flex';
            const title = document.getElementById('authTitle');
            const nickGroup = document.getElementById('nicknameGroup');
            const forgot = document.getElementById('forgotSection');
            const form = document.getElementById('authForm');

            if(type === 'login') {
                form.action = '/login';
                title.innerText = 'CORE LOGIN';
                nickGroup.style.display = 'none';
                forgot.style.display = 'block';
            } else {
                form.action = '/register';
                title.innerText = 'NEW RECRUIT';
                nickGroup.style.display = 'block';
                forgot.style.display = 'none';
            }
        }

        function closeAuth() {
            document.getElementById('authOverlay').style.display = 'none';
        }

        function toggleForgot() {
            const ff = document.getElementById('forgotForm');
            ff.style.display = ff.style.display === 'none' ? 'block' : 'none';
        }

        function copyContract() {
            const el = document.createElement('textarea');
            el.value = '0xB47FD3EFAcDdB316685D14B89598D78B4ce25181';
            document.body.appendChild(el);
            el.select();
            document.execCommand('copy');
            document.body.removeChild(el);
            alert('Contract Address Copied!');
        }
    </script>
</body>
</html>
