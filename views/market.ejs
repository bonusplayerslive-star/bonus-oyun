<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>BPL ULTIMATE MARKET</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Rajdhani:wght@500;700&family=Orbitron:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root { --neon-green: #39FF14; --dark-bg: #0a0a0a; --gold: #FFD700; }
        body { 
            background: var(--dark-bg); color: white; font-family: 'Rajdhani', sans-serif;
            background: url('/caracter/page/welcome.jpg') no-repeat center center fixed;
            background-size: cover; min-height: 100vh; margin: 0;
        }
        body::before { content: ""; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.9); z-index: -1; }

        .market-header {
            height: 10vh; background: rgba(0,0,0,0.95); border-bottom: 2px solid var(--neon-green);
            display: flex; align-items: center; position: sticky; top: 0; z-index: 1000; backdrop-filter: blur(10px);
        }
        .balance-box { border: 1px solid var(--neon-green); border-radius: 8px; padding: 5px 15px; color: var(--neon-green); font-family: 'Orbitron'; }
        
        .market-container { min-height: 90vh; padding: 20px 10px; }
        .animal-card {
            background: rgba(20, 20, 20, 0.95); border-radius: 15px; padding: 15px;
            border: 1px solid rgba(57, 255, 20, 0.15); transition: 0.3s; height: 100%;
        }
        .animal-img { width: 100%; aspect-ratio: 1/1; object-fit: cover; border-radius: 10px; border: 1px solid #333; margin-bottom: 15px; }
        .animal-name { font-family: 'Orbitron'; color: var(--neon-green); font-size: 1.2rem; }
        .price-tag { color: var(--gold); font-family: 'Orbitron'; font-size: 1.1rem; display: block; margin: 10px 0; }
        
        .btn-buy { 
            background: transparent; border: 1px solid var(--neon-green); color: var(--neon-green);
            width: 100%; padding: 12px; font-family: 'Orbitron'; transition: 0.5s;
        }
        .btn-buy:hover { background: var(--neon-green); color: black; box-shadow: 0 0 20px var(--neon-green); }
    </style>
</head>
<body>
    <div class="market-header">
        <div class="container d-flex justify-content-between align-items-center">
            <a href="/profil" class="text-white"><i class="fas fa-arrow-left fa-lg"></i></a>
            <h2 class="mb-0 font-orbitron text-uppercase" style="font-size: 1.2rem;">BPL MARKET</h2>
            <div class="balance-box"><%= user.balance.toLocaleString() %> BPL</div>
        </div>
    </div>

    <div class="container market-container">
        <div class="row g-3">
            <% const animals = [{name:'GORILLA', price: 5000, hp: 100, atk: 85}, {name:'LION', price: 4000, hp: 90, atk: 90}, {name:'EAGLE', price: 3000, hp: 70, atk: 95}]; %>
            <% animals.forEach(animal => { %>
            <div class="col-6 col-md-4">
                <div class="animal-card">
                    <img src="/caracter/profile/<%= animal.name %>.jpg" onerror="this.src='/caracter/page/logo.jpeg'" class="animal-img">
                    <div class="animal-name text-center"><%= animal.name %></div>
                    <div class="price-tag text-center"><%= animal.price %> BPL</div>
                    <button class="btn-buy" onclick="buyAnimal('<%= animal.name %>')">SATIN AL</button>
                </div>
            </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
