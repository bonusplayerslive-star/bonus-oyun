<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>BPL ULTIMATE MARKET</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Rajdhani:wght@500;700&family=Orbitron:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root { --neon-green: #39FF14; --dark-bg: #0a0a0a; --gold: #FFD700; }
        body { 
            background: var(--dark-bg); color: white; font-family: 'Rajdhani', sans-serif;
            background: url('/caracter/page/welcome.jpg') no-repeat center center fixed;
            background-size: cover; margin: 0; min-height: 100vh; overflow-x: hidden;
        }
        body::before { content: ""; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.9); z-index: -1; }

        .market-header {
            height: 10vh; background: rgba(0,0,0,0.95); border-bottom: 2px solid var(--neon-green);
            display: flex; align-items: center; position: sticky; top: 0; z-index: 1000; backdrop-filter: blur(10px);
        }
        .balance-box { background: rgba(57, 255, 20, 0.1); border: 1px solid var(--neon-green); border-radius: 8px; padding: 5px 12px; font-family: 'Orbitron'; color: var(--neon-green); }

        .market-content { height: 90vh; overflow-y: auto; padding: 20px 10px; }
        .animal-card {
            background: rgba(20, 20, 20, 0.9); border-radius: 15px; padding: 12px;
            text-align: center; border: 1px solid rgba(57, 255, 20, 0.1); transition: 0.3s;
            display: flex; flex-direction: column; height: 100%;
        }
        .animal-img { width: 100%; aspect-ratio: 1/1; object-fit: cover; border-radius: 10px; border: 2px solid #333; margin-bottom: 10px; }
        .price-tag { color: var(--gold); font-family: 'Orbitron'; font-weight: bold; margin: 10px 0; display: block; }
        
        .btn-buy { 
            background: transparent; border: 1px solid var(--neon-green); color: var(--neon-green);
            width: 100%; padding: 10px; font-family: 'Orbitron'; font-size: 0.8rem; transition: 0.3s;
        }
        .btn-buy:hover { background: var(--neon-green); color: black; box-shadow: 0 0 15px var(--neon-green); }
    </style>
</head>
<body>
    <div class="market-header">
        <div class="container d-flex justify-content-between align-items-center">
            <h4 class="mb-0 font-orbitron text-uppercase" style="font-size: 1rem;">BPL MARKET</h4>
            <div class="balance-box">
                <i class="fas fa-coins me-1"></i><%= (user.bpl || user.balance || 0).toLocaleString() %>
            </div>
        </div>
    </div>

    <div class="container market-content">
        <div class="row g-3">
            <% const items = [
                {name: 'GORILLA', price: 5000, hp: 100, atk: 80},
                {name: 'LION', price: 4500, hp: 90, atk: 85},
                {name: 'EAGLE', price: 3500, hp: 70, atk: 95}
            ]; %>
            <% items.forEach(item => { %>
            <div class="col-6 col-md-4">
                <div class="animal-card">
                    <img src="/caracter/profile/<%= item.name %>.jpg" onerror="this.src='/caracter/page/logo.jpeg'" class="animal-img">
                    <span class="font-orbitron" style="font-size: 0.9rem;"><%= item.name %></span>
                    <span class="price-tag"><%= item.price %> BPL</span>
                    <button class="btn-buy" onclick="buyItem('<%= item.name %>')">SATIN AL</button>
                </div>
            </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
