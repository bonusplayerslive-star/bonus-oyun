<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>BPL ARENA | ÖLÜMCÜL KARŞILAŞMA</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="/socket.io/socket.io.js"></script>
    
    <style>
        :root { --neon-green: #39FF14; --neon-red: #ff003c; --neon-yellow: #ffc107; }
        
        body { 
            background: #000; 
            color: #fff; 
            font-family: 'Orbitron', sans-serif; 
            min-height: 100vh;
            margin: 0;
            overflow-x: hidden;
            overflow-y: auto;
        }

        #video-overlay { 
            display: none; 
            position: fixed; 
            inset: 0; 
            background: rgba(0,0,0,0.98); 
            z-index: 9999; 
            flex-direction: column; 
            align-items: center; 
            justify-content: center; 
            padding: 10px;
        }

        .battle-view { 
            width: 100%; 
            max-width: 950px; 
            border: 2px solid var(--neon-green); 
            position: relative; 
            box-shadow: 0 0 30px rgba(57, 255, 20, 0.2); 
        }

        video { width: 100%; display: block; filter: contrast(1.1) brightness(1.1); }

        .player-header { 
            display: flex; 
            justify-content: space-between; 
            width: 100%; 
            max-width: 950px; 
            margin-bottom: 15px; 
        }

        .char-card { text-align: center; width: 30%; }
        .char-card img { 
            width: 60px; height: 60px; 
            border-radius: 50%; 
            border: 2px solid var(--neon-green); 
            margin-bottom: 5px; 
            object-fit: cover; 
        }

        .vs-label { 
            font-size: 1.5rem; 
            color: var(--neon-red); 
            text-shadow: 0 0 10px var(--neon-red); 
            align-self: center; 
        }

        .bet-card { 
            background: rgba(20,20,20,0.8); 
            border: 1px solid #333; 
            transition: 0.3s; 
            cursor: pointer; 
            padding: 15px 5px; 
            border-radius: 10px; 
            text-align: center;
            height: 100%;
        }

        .bet-card h3 { font-size: 1.1rem; margin-bottom: 5px; }
        .bet-card p { font-size: 0.8rem; margin: 0; }
        .bet-card:hover { border-color: var(--neon-green); }
        .bet-card.active { 
            border-color: var(--neon-green); 
            box-shadow: 0 0 15px rgba(57, 255, 20, 0.4); 
            background: #111; 
        }

        .win-banner { 
            position: absolute; top: 50%; left: 50%; 
            transform: translate(-50%, -50%) scale(0); 
            font-size: 2.5rem; font-weight: 900; 
            z-index: 10001; text-align: center; 
            transition: 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275); 
            width: 100%; 
        }

        .banner-active { transform: translate(-50%, -50%) scale(1); }
        #timer { font-size: 4rem; color: var(--neon-red); text-shadow: 0 0 20px var(--neon-red); }
        .status-text { margin-top: 10px; color: var(--neon-green); font-size: 0.9rem; font-weight: bold; }
    </style>
</head>
<body>

    <div id="video-overlay">
        <div class="player-header">
            <div class="char-card">
                <img src="/caracter/profile/<%= user.selectedAnimal %>.png">
                <div class="text-white small" style="font-size: 0.7rem;"><%= user.nickname %></div>
            </div>
            <div class="vs-label">VS</div>
            <div class="char-card">
                <img id="opp-img" src="/img/searching.png">
                <div id="opp-name" class="text-white small" style="font-size: 0.7rem;">ARANIYOR...</div>
            </div>
        </div>
        <div class="battle-view">
            <div id="result-banner" class="win-banner"></div>
            <video id="battle-video" playsinline muted autoplay></video>
        </div>
        <div id="battle-status" class="status-text text-uppercase">BAĞLANTI KURULUYOR...</div>
    </div>

    <div class="container py-4">
        <div id="lobby-ui" class="text-center">
            <h1 class="mb-4 text-uppercase" style="color:var(--neon-green); text-shadow: 0 0 10px var(--neon-green);">ARENA</h1>
            
            <div id="bet-selection">
                <h4 class="mb-4 text-white-50">SALDIRI MODU SEÇİN</h4>
                <div class="row g-2 justify-content-center">
                    <div class="col-6 col-md-3" onclick="setMultiplier(1, this)">
                        <div class="bet-card active"><h3>NORMAL</h3><p>25 BPL</p></div>
                    </div>
                    <div class="col-6 col-md-3" onclick="setMultiplier(2, this)">
                        <div class="bet-card"><h3>2X GÜÇ</h3><p>55 BPL</p></div>
                    </div>
                    <div class="col-6 col-md-3" onclick="setMultiplier(4, this)">
                        <div class="bet-card"><h3>4X ÖFKE</h3><p>75 BPL</p></div>
                    </div>
                    <div class="col-6 col-md-3" onclick="setMultiplier(6, this)">
                        <div class="bet-card"><h3 style="color: #ff00ff;">6X MAX</h3><p>85 BPL</p></div>
                    </div>
                </div>
                
                <div class="mt-5">
                    <button id="start-btn" onclick="startSearch()" class="btn btn-danger btn-lg px-5 py-3 fw-bold w-100">
                        SALDIRIYI BAŞLAT
                    </button>
                </div>
            </div>

            <div id="matching-ui" style="display:none;">
                <div id="timer">13</div>
                <h3 class="mt-3" style="color:var(--neon-green); font-size: 1.2rem;">RAKİP ARANIYOR...</h3>
                <div class="spinner-border text-success mt-3" role="status"></div>
                <div class="mt-3 text-white-50 small">Rastgele eşleşme veya Bot bekleniyor...</div>
            </div>
        </div>
    </div>
<script>
// arena.ejs içindeki script kısmı
async function handleBattle(data) {
    isMatchFound = true;
    clearInterval(timerInt);
    
    // Oyuncu Bilgileri
    const me = data.players.find(p => p.nick === myNick);
    const opponent = data.players.find(p => p.nick !== myNick);

    document.getElementById('matching-ui').style.display = 'none';
    document.getElementById('opp-name').innerText = opponent.nick;
    document.getElementById('opp-img').src = opponent.img; // Dinamik resim yolu
    
    document.getElementById('video-overlay').style.display = 'flex';

    // SIRA 1: Rakibin Saldırısı (Herkes rakibin hamlesini izler)
    // Örn: Tiger1.mp4
    await playVideoAsync(opponent.animal, `${opponent.animal}1`, `${opponent.nick.toUpperCase()} HAMLE YAPIYOR!`);

    // SIRA 2: Kazananın Zafer Videosu
    // Örn: Tiger.mp4
    const winner = data.isWin ? me : opponent;
    
    // UI Güncelleme
    resultBanner.innerText = data.isWin ? "VICTORY" : "DEFEAT";
    resultBanner.style.color = data.isWin ? "var(--neon-green)" : "var(--neon-red)";
    resultBanner.classList.add('banner-active');

    await playVideoAsync(winner.animal, winner.animal, `${winner.nick.toUpperCase()} KAZANDI!`);
    
    // 3 Saniye bekle ve profile dön
    setTimeout(() => {
        window.location.href = "/profil";
    }, 3000);
}

// arena.ejs içinde
function playVideoAsync(animal, file, text) {
    return new Promise((resolve) => {
        statusMsg.innerText = text;
        
        // Klasör ismi için: tiger -> Tiger
        const formattedAnimal = animal.charAt(0).toUpperCase() + animal.slice(1).toLowerCase();
        
        // Dosya ismi için: tiger1 -> Tiger1 (Eğer file verisi küçük gelirse diye)
        const formattedFile = file.charAt(0).toUpperCase() + file.slice(1);
        
        // Sonuç: /caracter/move/Tiger/Tiger1.mp4
        videoElement.src = `/caracter/move/${formattedAnimal}/${formattedFile}.mp4`;
        
        videoElement.onended = resolve;
        videoElement.play().catch(e => {
            console.error("Video oynatılamadı, 2 saniye bekleniyor...", e);
            // Video yüklenmezse oyun takılmasın diye 2 saniye sonra devam et
            setTimeout(resolve, 2000); 
        });
    });
}
<video id="arenaVideo" preload="auto" ...></video>
    });
}
    });
}
    });
}
</script>
</body>
</html>




