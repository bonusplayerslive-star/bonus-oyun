<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BPL PROFİL | <%= user.nickname %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Rajdhani:wght@500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --neon-green: #39FF14; --dark-bg: #050505; --glass: rgba(0, 0, 0, 0.85); --gold: #ffc107; --neon-blue: #00d4ff; }
        body { background: var(--dark-bg); color: white; font-family: 'Rajdhani', sans-serif; overflow-x: hidden; }
        
        /* Arkaplan Atmosferi */
        body::before {
            content: ""; position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: radial-gradient(circle at center, #112211 0%, #050505 100%);
            z-index: -1;
        }

        /* Header Üst Panel */
        .header-section { 
            background: rgba(0,0,0,0.95); border-bottom: 2px solid var(--neon-green);
            padding: 15px 0; box-shadow: 0 0 20px rgba(57, 255, 20, 0.2);
        }
        
        .glass-panel {
            background: var(--glass); border: 1px solid rgba(57, 255, 20, 0.15);
            border-radius: 20px; padding: 25px; height: 100%; box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }
        
        /* Karakter Kartları */
        .stat-card {
            background: rgba(20, 20, 20, 0.8); border: 1px solid rgba(255,255,255,0.05);
            border-radius: 15px; padding: 15px; transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative; overflow: hidden;
        }
        .stat-card:hover { border-color: var(--neon-green); transform: translateY(-8px); box-shadow: 0 0 25px rgba(57, 255, 20, 0.2); }
        
        .inventory-img { 
            width: 100%; aspect-ratio: 1/1; object-fit: cover; 
            border-radius: 12px; border: 2px solid rgba(255,255,255,0.1); transition: 0.3s;
        }
        .selected-animal-card { border: 2px solid var(--gold) !important; background: rgba(255, 193, 7, 0.05) !important; }
        .selected-animal-card .inventory-img { border-color: var(--gold); box-shadow: 0 0 15px var(--gold); }

        /* Neon Butonlar */
        .nav-btn { 
            width: 100%; margin-bottom: 15px; font-family: 'Orbitron'; font-size: 0.75rem; 
            letter-spacing: 1px; padding: 12px; border-radius: 10px; text-transform: uppercase;
        }
        .btn-market-special {
            background: var(--neon-green); color: black; border: none; font-weight: 900;
            box-shadow: 0 0 15px var(--neon-green); animation: pulse 2s infinite;
        }

        /* Stat Barları */
        .progress { background: #111; height: 6px; border-radius: 10px; overflow: visible; margin-top: 5px; }
        .stat-label { font-size: 0.65rem; color: #888; font-family: 'Orbitron'; display: flex; justify-content: space-between; }

        @keyframes pulse {
            0% { transform: scale(1); box-shadow: 0 0 10px var(--neon-green); }
            50% { transform: scale(1.02); box-shadow: 0 0 25px var(--neon-green); }
            100% { transform: scale(1); box-shadow: 0 0 10px var(--neon-green); }
        }
    </style>
</head>
<body>

    <div class="header-section mb-4">
        <div class="container d-flex justify-content-between align-items-center">
            <div class="d-flex align-items-center">
                <img src="/caracter/page/logo.jpeg" height="45" class="me-3 rounded border border-success">
                <h4 class="font-orbitron mb-0 d-none d-md-block" style="color: var(--neon-green); letter-spacing: 2px;">BPL COMMAND CENTER</h4>
            </div>
            <div class="text-end">
                <div class="text-muted small font-orbitron">OPERATÖR</div>
                <div class="h5 mb-0 text-white"><%= user.nickname %> <span class="ms-2 text-warning" style="font-family: 'Orbitron';"><i class="fas fa-coins"></i> <%= user.bpl.toLocaleString() %></span></div>
            </div>
        </div>
    </div>

    <div class="container">
        <div class="row">
            <div class="col-lg-3 mb-4">
                <div class="glass-panel">
                    <h6 class="text-muted border-bottom border-secondary pb-2 mb-4 font-orbitron text-center">NAVİGASYON</h6>
                    
                    <% if (user.inventory && user.inventory.length >= 3) { %>
                        <button class="btn btn-secondary nav-btn opacity-50 cursor-not-allowed">
                            <i class="fas fa-lock me-2"></i>ENVANTER DOLU
                        </button>
                    <% } else { %>
                        <a href="/market" class="btn btn-market-special nav-btn"><i class="fas fa-shopping-cart me-2"></i>MARKET</a>
                    <% } %>

                    <a href="/arena" class="btn btn-outline-danger nav-btn"><i class="fas fa-skull-crossbones me-2"></i>ARENA SAVAŞI</a>
                    <a href="/chat" class="btn btn-outline-info nav-btn"><i class="fas fa-satellite-dish me-2"></i>STRATEJİ ODASI</a>
                    <a href="/meeting" class="btn btn-outline-warning nav-btn"><i class="fas fa-users-cog me-2"></i>BEŞGEN MASA</a>
                    <hr class="border-secondary my-4">
                    <a href="/logout" class="btn btn-link text-danger nav-btn text-decoration-none"><i class="fas fa-power-off me-2"></i>SİSTEMDEN ÇIK</a>
                </div>
            </div>

            <div class="col-lg-9">
                <div class="glass-panel">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h4 class="font-orbitron mb-0"><i class="fas fa-dna text-success me-2"></i>BİYOLOJİK VARLIKLARINIZ</h4>
                        <span class="badge bg-dark border border-success text-success px-3 py-2 font-orbitron">
                            <%= user.inventory.length %> / 3 KAPASİTE
                        </span>
                    </div>

                    <div class="row">
                        <% if(!user.inventory || user.inventory.length === 0) { %>
                            <div class="col-12 text-center py-5">
                                <div class="mb-4 opacity-25"><i class="fas fa-box-open fa-5x"></i></div>
                                <h5 class="font-orbitron text-muted">VARLIK TESPİT EDİLEMEDİ</h5>
                                <p class="text-muted small">Arenada savaşmak için marketten bir karakter edinmelisin.</p>
                                <a href="/market" class="btn btn-success px-5 mt-2 font-orbitron">MARKETE GİT</a>
                            </div>
                        <% } else { %>
                            <% user.inventory.forEach((item) => { %>
                                <div class="col-md-4 mb-4">
                                    <div class="stat-card <%= user.selectedAnimal === item.name ? 'selected-animal-card' : '' %>">
                                        <div class="position-relative">
                                            <img src="/caracter/profile/<%= item.name %>.jpg" onerror="this.src='/caracter/page/logo.jpeg'" class="inventory-img">
                                            <div class="position-absolute bottom-0 end-0 bg-black bg-opacity-75 px-2 py-1 m-2 rounded border border-info font-orbitron" style="font-size: 0.7rem;">
                                                LVL <%= item.level || 1 %>
                                            </div>
                                        </div>

                                        <h5 class="text-uppercase text-center my-3 font-orbitron text-info"><%= item.name %></h5>
                                        
                                        <div class="stat-label"><span>HP</span><span><%= item.hp %> / <%= item.maxHp %></span></div>
                                        <div class="progress mb-2"><div class="progress-bar bg-danger" style="width: <%= (item.hp/item.maxHp)*100 %>%"></div></div>

                                        <div class="stat-label"><span>ENERJİ</span><span>%<%= item.stamina %></span></div>
                                        <div class="progress mb-4"><div class="progress-bar bg-warning" style="width: <%= item.stamina %>%"></div></div>

                                        <button onclick="selectForArena('<%= item.name %>')" 
                                            class="btn w-100 font-orbitron py-2 <%= user.selectedAnimal === item.name ? 'btn-success' : 'btn-outline-warning' %>" 
                                            style="font-size: 0.7rem;">
                                            <%= user.selectedAnimal === item.name ? 'SAVAŞA HAZIR' : 'SAVAŞÇI OLARAK SEÇ' %>
                                        </button>
                                    </div>
                                </div>
                            <% }) %>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        async function selectForArena(name) {
            Swal.fire({ title: 'İşleniyor...', allowOutsideClick: false, didOpen: () => Swal.showLoading() });
            
            try {
                const res = await fetch('/api/select-animal', {
                    method: 'POST',
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify({ animalName: name })
                });
                const data = await res.json();
                
                if(data.success) {
                    Swal.fire({
                        icon: 'success', title: 'ONAYLANDI',
                        text: name + ' bir sonraki savaş için seçildi.',
                        background: '#050505', color: '#fff', confirmButtonColor: '#39FF14'
                    }).then(() => location.reload());
                } else {
                    Swal.fire('HATA', data.error, 'error');
                }
            } catch (err) {
                Swal.fire('HATA', 'Sunucu bağlantısı sağlanamadı.', 'error');
            }
        }
    </script>
</body>
</html>
