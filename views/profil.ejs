<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BPL PROFİL | <%= user.nickname %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Rajdhani:wght@300;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root { --neon-green: #39FF14; --dark-bg: #050505; --glass: rgba(0, 0, 0, 0.85); --gold: #ffc107; }
        body { background: var(--dark-bg); color: white; font-family: 'Rajdhani', sans-serif; overflow-x: hidden; }
        
        body::before {
            content: ""; position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: url('/caracter/page/welcome.jpg') no-repeat center center fixed;
            background-size: cover; opacity: 0.15; z-index: -1;
        }

        .header-section { 
            background: rgba(0,0,0,0.9); border-bottom: 2px solid var(--neon-green);
            padding: 15px 0; margin-bottom: 30px; backdrop-filter: blur(10px);
        }
        .logo-img { height: 50px; filter: drop-shadow(0 0 5px var(--neon-green)); }
        
        .glass-panel {
            background: var(--glass); border: 1px solid rgba(57, 255, 20, 0.2);
            border-radius: 15px; padding: 20px; height: 100%;
        }
        
        .stat-card {
            background: rgba(0,0,0,0.7); border: 1px solid rgba(255,255,255,0.1);
            border-radius: 12px; padding: 15px; transition: 0.3s; margin-bottom: 20px;
        }
        .stat-card:hover { border-color: var(--neon-green); transform: translateY(-5px); }
        
        /* Resimlerin düzgün görünmesi için yol düzeltmesi */
        .inventory-img { 
            width: 100%; aspect-ratio: 1/1; object-fit: cover; 
            border-radius: 10px; border: 2px solid var(--neon-green); margin-bottom: 10px;
        }

        /* Seçili hayvanı vurgulamak için efekt */
        .selected-animal-card { border: 2px solid var(--gold) !important; box-shadow: 0 0 15px var(--gold); }
        
        .bpl-badge { color: var(--neon-green); font-family: 'Orbitron'; font-weight: bold; font-size: 1.3rem; }
        .nav-btn { width: 100%; margin-bottom: 12px; font-weight: bold; font-family: 'Orbitron'; font-size: 0.8rem; letter-spacing: 1px; transition: 0.3s; }
        
        .progress { background: #1a1a1a; border-radius: 10px; border: 1px solid #333; height: 8px; }
        .stat-label { font-size: 0.7rem; color: #aaa; font-family: 'Orbitron'; text-transform: uppercase; }

        .btn-market-special {
            background: var(--neon-green); color: black; border: none;
            box-shadow: 0 0 15px var(--neon-green); animation: pulse 2s infinite;
        }
        @keyframes pulse {
            0% { box-shadow: 0 0 5px var(--neon-green); }
            50% { box-shadow: 0 0 20px var(--neon-green); }
            100% { box-shadow: 0 0 5px var(--neon-green); }
        }

        .action-container {
            background: rgba(0,0,0,0.5); border-radius: 8px;
            padding: 12px; margin-top: 15px; border: 1px solid rgba(255,255,255,0.05);
        }
        
        .btn-arena-select { background: var(--gold); color: #000; font-weight: bold; border: none; }
        .btn-arena-select.active { background: #28a745; color: #fff; }
        .btn-dev-select { background: transparent; border: 1px solid #0dcaf0; color: #0dcaf0; margin-top: 8px; }
        .btn-dev-select:hover { background: #0dcaf0; color: black; }

        .font-orbitron { font-family: 'Orbitron'; }
    </style>
</head>
<body>

    <div class="header-section">
        <div class="container d-flex justify-content-between align-items-center">
            <a href="/profil"><img src="/caracter/page/logo.jpeg" alt="BPL LOGO" class="logo-img"></a>
            <div class="text-end">
                <h5 class="mb-0 text-uppercase font-orbitron" style="color: var(--neon-green);"><%= user.nickname %></h5>
                <span class="bpl-badge"><i class="fas fa-coins me-1"></i><%= (user.bpl || 0).toLocaleString() %> BPL</span>
            </div>
        </div>
    </div>

    <div class="container">
        <div class="row">
            <div class="col-md-3 mb-4">
                <div class="glass-panel text-center">
                    <h6 class="text-muted border-bottom border-secondary pb-2 mb-3 font-orbitron">SİSTEM MENÜSÜ</h6>
                    <a href="/market" class="btn btn-market-special nav-btn mb-4"><i class="fas fa-shopping-cart me-2"></i>BPL MARKET</a>
                    <a href="/development" class="btn btn-outline-warning nav-btn"><i class="fas fa-tools me-2"></i>GELİŞTİRME</a>
                    <a href="/arena" class="btn btn-outline-danger nav-btn"><i class="fas fa-crosshairs me-2"></i>ARENA (BATTLE)</a>
                    <a href="/chat" class="btn btn-outline-info nav-btn"><i class="fas fa-comments me-2"></i>GLOBAL CHAT</a>
                    <a href="/meeting" class="btn btn-outline-primary nav-btn" style="border-color: #ffc107; color: #ffc107;">
                        <i class="fas fa-video me-2"></i>BEŞGEN MASA
                    </a>
                    <a href="/wallet" class="btn btn-outline-light nav-btn"><i class="fas fa-wallet me-2"></i>CÜZDAN</a>
                    <hr class="border-secondary">
                    <a href="/logout" class="btn btn-dark nav-btn text-danger border-danger"><i class="fas fa-power-off me-2"></i>ÇIKIŞ YAP</a>
                </div>
            </div>

            <div class="col-md-9">
                <div class="glass-panel">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h4 class="font-orbitron mb-0 text-uppercase"><i class="fas fa-boxes me-2 text-success"></i>ENVANTER</h4>
                        <span class="badge rounded-pill bg-dark border border-secondary p-2">Varlık: <%= (user.inventory ? user.inventory.length : 0) %></span>
                    </div>

                    <div class="row">
                        <% if(!user.inventory || user.inventory.length === 0) { %>
                            <div class="col-12 text-center py-5">
                                <i class="fas fa-dragon fa-4x mb-3 text-secondary" style="opacity: 0.2;"></i>
                                <h3 class="font-orbitron">ENVANTER BOŞ</h3>
                                <p class="text-muted">Henüz bir karakterin yok. Marketten hemen bir tane edin!</p>
                                <a href="/market" class="btn btn-outline-success mt-3">KARAKTER SATIN AL</a>
                            </div>
                        <% } else { %>
                            <% user.inventory.forEach((item) => { %>
                                <div class="col-md-4 mb-4">
                                    <div class="stat-card text-center <%= user.selectedAnimal === item.name ? 'selected-animal-card' : '' %>">
                                        <div style="position: relative;">
                                            <img src="/caracter/profile/<%= item.name %>.jpg" onerror="this.src='/caracter/page/logo.jpeg'" class="inventory-img">
                                            <span style="position: absolute; top: 10px; right: 10px; background: var(--neon-green); color: black; padding: 2px 8px; border-radius: 5px; font-weight: bold; font-family: 'Orbitron'; font-size: 0.7rem;">
                                                LVL <%= item.level || 1 %>
                                            </span>
                                        </div>

                                        <h5 class="text-uppercase my-2 font-orbitron" style="color: var(--neon-green);"><%= item.name %></h5>
                                        
                                        <div class="px-2">
                                            <% 
                                                let hpVal = item.hp || 100;
                                                let maxHpVal = item.maxHp || 100;
                                                let hpPercent = (hpVal / maxHpVal) * 100;
                                                let staPercent = (item.stamina || 100);
                                            %>
                                            <div class="d-flex justify-content-between stat-label mb-1">
                                                <span>HP</span>
                                                <span><%= hpVal %> / <%= maxHpVal %></span>
                                            </div>
                                            <div class="progress mb-2">
                                                <div class="progress-bar bg-danger" style="width: <%= hpPercent %>%"></div>
                                            </div>

                                            <div class="d-flex justify-content-between stat-label mb-1">
                                                <span>STAMINA</span>
                                                <span>%<%= staPercent %></span>
                                            </div>
                                            <div class="progress mb-3">
                                                <div class="progress-bar bg-warning" style="width: <%= staPercent %>%"></div>
                                            </div>

                                            <div class="action-container">
                                                <button onclick="selectForArena('<%= item.name %>')" class="btn btn-arena-select btn-sm w-100 font-orbitron mb-2 <%= user.selectedAnimal === item.name ? 'active' : '' %>">
                                                    <%= user.selectedAnimal === item.name ? 'SEÇİLİ KARAKTER' : 'ARENA İÇİN SEÇ' %>
                                                </button>
                                                <button onclick="window.location.href='/development'" class="btn btn-dev-select btn-sm w-100 font-orbitron">
                                                    GELİŞTİRME YAP
                                                </button>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            <% }) %>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        async function selectForArena(name) {
            try {
                // app.js'deki API yoluyla eşitlendi: /api/select-animal
                const res = await fetch('/api/select-animal', {
                    method: 'POST',
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify({ animalName: name }) // İsim gönderiyoruz
                });
                
                const data = await res.json();
                
                if(data.success) {
                    Swal.fire({
                        title: 'BAŞARILI!',
                        text: name + ' savaşa hazırlandı.',
                        icon: 'success',
                        background: '#050505',
                        color: '#fff',
                        confirmButtonColor: '#39FF14'
                    }).then(() => {
                        location.reload(); // Sayfayı yenile ki "Seçili" görünsün
                    });
                } else {
                    Swal.fire('Hata', data.error || 'Seçim yapılamadı.', 'error');
                }
            } catch (err) {
                console.error("Hata:", err);
                Swal.fire('Hata', 'Sunucu bağlantısı koptu.', 'error');
            }
        }
    </script>
</body>
</html>
