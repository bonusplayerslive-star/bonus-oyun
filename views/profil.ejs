<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BPL PROFİL | <%= user.nickname %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root { --neon-green: #39FF14; --dark-bg: #0a0a0a; }
        body { background: var(--dark-bg); color: white; font-family: 'Rajdhani', sans-serif; }
        
        .header-section { 
            background: linear-gradient(45deg, #000, #111); 
            border-bottom: 2px solid var(--neon-green);
            padding: 20px 0; 
            margin-bottom: 30px;
        }
        .logo-img { height: 60px; filter: drop-shadow(0 0 5px var(--neon-green)); }
        
        .stat-card {
            background: rgba(255,255,255,0.05);
            border: 1px solid #333;
            border-radius: 15px;
            padding: 20px;
            transition: 0.3s;
        }
        .stat-card:hover { border-color: var(--neon-green); transform: translateY(-5px); }
        
        .inventory-img { 
            width: 100%; 
            aspect-ratio: 1/1; 
            object-fit: cover; 
            border-radius: 10px; 
            border: 1px solid #444;
        }
        
        .bpl-badge { color: var(--neon-green); font-weight: bold; font-size: 1.2rem; }
        .limit-alert { font-size: 0.8rem; color: #ff4444; border: 1px solid #ff4444; padding: 5px; border-radius: 5px; }
        
        .nav-btn { width: 100%; margin-bottom: 10px; text-transform: uppercase; font-weight: bold; }
        .btn-meeting { border: 2px solid #ffc107; color: #ffc107; }
        .btn-meeting:hover { background: #ffc107; color: #000; }
    </style>
</head>
<body>

    <div class="header-section">
        <div class="container d-flex justify-content-between align-items-center">
            <img src="/caracter/logo/logo.jpeg" alt="BPL LOGO" class="logo-img" onerror="this.src='/caracter/page/logo.jpeg'">
            <div class="text-end">
                <h4 class="mb-0 text-neon"><%= user.nickname %></h4>
                <span class="bpl-badge"><%= user.bpl.toLocaleString() %> BPL</span>
            </div>
        </div>
    </div>

    <div class="container">
        <div class="row">
            <div class="col-md-3 mb-4">
                <div class="stat-card">
                    <h5 class="text-muted border-bottom pb-2 mb-3">MENÜ</h5>
                    <a href="/market" class="btn btn-outline-success nav-btn"><i class="fas fa-shopping-cart"></i> MARKET</a>
                    <a href="/arena" class="btn btn-outline-danger nav-btn"><i class="fas fa-swords"></i> ARENA</a>
                    <a href="/chat" class="btn btn-outline-info nav-btn"><i class="fas fa-comments"></i> GLOBAL CHAT</a>
                    
                    <form action="/create-meeting" method="POST" style="width: 100%;">
                        <button type="submit" class="btn btn-meeting nav-btn">
                            <i class="fas fa-video"></i> BEŞGEN MASA (50 BPL)
                        </button>
                    </form>

                    <a href="/wallet" class="btn btn-light nav-btn"><i class="fas fa-wallet"></i> WALLET (CÜZDAN)</a>
                    <hr>
                    <a href="/" class="btn btn-dark nav-btn text-danger">ÇIKIŞ YAP</a>
                </div>
            </div>

            <div class="col-md-9">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h3>ENVANTER (ASSETS)</h3>
                    <% if(user.inventory.length >= 3) { %>
                        <span class="limit-alert">ÇANTA DOLU (MAX 3)</span>
                    <% } else { %>
                        <span class="text-muted">Kapasite: <%= user.inventory.length %>/3</span>
                    <% } %>
                </div>

                <div class="row">
                    <% if(user.inventory.length === 0) { %>
                        <div class="col-12 text-center p-5 border border-dashed text-muted">
                            Henüz bir hayvanınız yok. Marketten hemen bir tane edinin!
                        </div>
                    <% } else { %>
                        <% user.inventory.forEach(item => { %>
                            <div class="col-md-4 mb-4">
                                <div class="stat-card text-center">
                                    <img src="/caracter/profile/<%= item.toLowerCase() %>.jpg" 
                                         onerror="this.src='/caracter/page/logo.jpeg'" 
                                         class="inventory-img mb-3">
                                    <h5 class="text-uppercase"><%= item %></h5>
                                    <a href="/arena?animal=<%= item %>" class="btn btn-sm btn-danger w-100">SAVAŞA GİT</a>
                                </div>
                            </div>
                        <% }) %>
                    <% } %>
                </div>
            </div>
        </div>
    </div>

    <script src="/socket.io/socket.io.js"></script>
    <script>
        const socket = io();
        
        // Kayıt ve Online Takibi
        socket.emit('register-user', { 
            id: "<%= user._id %>", 
            nickname: "<%= user.nickname %>" 
        });

        // Arena Daveti Yakalama
        socket.on('challenge-received', (data) => {
            if(confirm(data.challenger + " seni Arena'ya davet ediyor! Kabul ediyor musun?")) {
                window.location.href = '/arena';
            }
        });

        // Globalden gelen Beşgen Masa davetleri için
        socket.on('receive-meeting-invite', (data) => {
            console.log("Masa daveti geldi:", data);
        });
    </script>
</body>
</html>
